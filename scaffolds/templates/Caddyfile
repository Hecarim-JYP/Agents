# 리버스 프록시 — HTTPS 종단 + 라우팅만 담당한다 (docker.md 7절).
# 정적 파일은 client(nginx) 컨테이너가 서빙한다 — 2026-07-14 확정.
# SITE_ADDRESS: 도메인이면 자동 HTTPS, 사내 도메인 미확보면 ":80" (체크리스트 15)

{$SITE_ADDRESS}

handle /api/* {
	reverse_proxy server:3000
}

handle /health {
	reverse_proxy server:3000
}

handle {
	reverse_proxy client:80
}
